apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-notifications-cm
  namespace: argocd
data:
  service.github: |
    appID: 410217 
    installationID: 43032092
    privateKey: $github-privateKey
  template.app-deployed: |
    message: |
      Application {{.app.metadata.name}} is now running new version of deployments manifests.
    github:
      repoURLPath: "{{.app.spec.source.repoURL}}"
      revisionPath: "{{.app.spec.source.targetRevision}}"
      status:
        state: success
        label: "argo-cd/{{.app.metadata.name}}"
        targetURL: "{{.context.argocdUrl}}/applications/{{.app.metadata.name}}?operation=true"
      pullRequestComment:
        content: |
          The application code for this pull requests has been deployed and is available at: [{{.app.metadata.name}}]({{.context.helloAppUrl}}).
          For possible deployment issues or for more info, view in the pull request specific [Argo CD instance]({{.context.argocdUrl}}/applications/{{.app.metadata.name}}?operation=true)
  trigger.on-deployed: |
    - description: Application is synced and healthy. Triggered once per commit.
      oncePer: app.status.operationState?.syncResult?.revision
      send:
      - app-deployed
      when: app.status.operationState.phase in ['Succeeded']
        and app.status.health.status == 'Healthy'
